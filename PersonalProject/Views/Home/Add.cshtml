@model RecipeViewModel

<h2>Add a Recipe:</h2>
<form asp-action="Add" method="post">
    <div asp-validation-summary="All" class="text-danger"></div>

    <div class="form-group">
        <label asp-for="@Model.Recipe.RecipeName">Recipe Name</label>
        <input asp-for="@Model.Recipe.RecipeName" class="form-control" />
    </div>

    <div>
        <label asp-for="@Model.RecipeIngredients[0].Amount">Amount</label>
        <input asp-for="@Model.RecipeIngredients[0].Amount" />
        <label asp-for="@Model.Ingredients[0].IngredientName">Ingredient</label>
        <input asp-for="@Model.Ingredients[0].IngredientName" class="form-control" />
    </div>

    <div>
        <label asp-for="@Model.Recipe.RecipeInstructions">Instructions:</label>
        <input asp-for="@Model.Recipe.RecipeInstructions" class="form-control" />
    </div>

    <input type="hidden" asp-for="@Model.Recipe.RecipeID" value="0" />
    <input type="hidden" asp-for="@Model.Recipe.UserId" value="@ViewBag.userId" />

    <button type="submit" class="btn btn-primary">Add Recipe</button>
    <a asp-asp-controller="Home" asp-action="ChooseRecipe" class="btn btn-primary">Cancel</a>

</form>