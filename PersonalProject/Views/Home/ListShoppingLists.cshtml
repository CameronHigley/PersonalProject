@model List<ShoppingList>
<ul>
    @for(int i = 0; i< Model.Count(); i++)
    {
        <li><a asp-action="ViewShoppingList" asp-route-id="@Model[i].Id" >@Model[i].Time</a></li>
    }
</ul>
<a asp-action="MakeShoppingList" class="btn btn-primary">Make new List</a>